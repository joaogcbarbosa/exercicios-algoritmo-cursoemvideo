Algoritmo "98"

Var

   x, y: inteiro
   SS: real
   
Função SuperSomador(a, b: inteiro): real
var

   termos, c: inteiro
   SomaTotal: real

Inicio

      se (a < b) entao
         para c <- a até b passo 1 faça:
              termos <- termos + 1
         FimPara
         SomaTotal <- ((a + b)*termos)/2
         retorne SomaTotal
      senao
          para c <- b até a passo 1 faça:
              termos <- termos + 1
         FimPara
         SomaTotal <- ((b + a)*termos)/2
         retorne SomaTotal
      fimse

FimFunção

Inicio

      Escreva("Primeiro número: ")
      Leia(x)
      Escreva("Último número: ")
      Leia(y)
      SS <- SuperSomador(x, y)
      Escreva("A soma de todos números de",x," até",y," é",SS)

Fimalgoritmo